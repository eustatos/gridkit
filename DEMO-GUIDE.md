# 🚀 Гайд: Как запустить и протестировать GridKit DevTools

## Быстрый старт (3 шага)

### 1. Запустите демо-приложение
```bash
pnpm dev:demo
```
Откроется `http://localhost:3000` с готовой таблицей.

### 2. Загрузите DevTools в Chrome
1. Откройте `chrome://extensions/`
2. Включите **Developer mode**
3. Нажмите **Load unpacked**
4. Выберите папку: `packages/devtools/extension`

### 3. Проверьте работу
- Откройте DevTools Extension (иконка GridKit в панели расширений)
- Вы увидите список таблиц
- Можно просматривать state, события, метрики

---

## Что можно протестировать

### В демо-приложении:
- ✅ Сортировка колонок (клик по заголовкам)
- ✅ Пагинация (нижняя панель)
- ✅ Выбор строк (чекбоксы)
- ✅ Переключение темы (Dark/Light)
- ✅ Уведомления (включить/выключить)

### В DevTools Extension:
- ✅ Просмотр текущего state таблицы
- ✅ Журнал событий
- ✅ Performance метрики
- ✅ Память и утечки
- ✅ Плагины

---

## Разработка DevTools

Если вы разрабатываете сам Extension:

1. **Пересоберите extension:**
```bash
cd packages/devtools
npm run build:extension
```

2. **В Chrome** нажмите кнопку "Обновить" для расширения (круглая стрелка)

3. **Обновите страницу** с демо-приложением

---

## Частые проблемы

### Расширение не загружается
- Убедитесь, что в `packages/devtools/extension/` есть `manifest.json`
- Проверьте, что есть `background.js`, `content.js`, `devtools.js`
- Убедитесь, что есть иконки (`.svg` файлы)

### DevTools не видит таблицу
- Убедитесь, что в таблице включена отладка: `debug: true`
- Проверьте консоль браузера на ошибки
- Убедитесь, что расширение загружено в Chrome

### Данные не обновляются
- Проверьте, что вызываются методы таблицы (`updateOptions`, `setData` и т.д.)
- Убедитесь, что включены уведомления в демо-приложении

---

## Полезные ссылки

- [GridKit Documentation](./docs/README.md)
- [DevTools Architecture](./packages/devtools/README.md)
- [Core Package](./packages/core/README.md)
