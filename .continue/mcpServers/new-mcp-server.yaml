name: MCP context server
version: 0.0.1
schema: v1
mcpServers:
  - name: MCP context server
    command: node
    args:
      - /Users/aleksanderastashkin/develop/mcp-module-server/dist/server-mcp.js
      - /Users/aleksanderastashkin/develop/gridkit
    env:
      NODE_ENV: production
      LOG_LEVEL: info
