// Setup JSDOM for React components testing
// jsdomSetup() - simplified for now

// Setup global test utilities
beforeEach(() => {
  // Clear any global state
  vi.clearAllMocks()
})

afterEach(() => {
  // Cleanup after each test
})
