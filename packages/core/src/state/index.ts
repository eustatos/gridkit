/**
 * GridKit State Store
 * 
 * This module provides a lightweight, reactive state store for table state management.
 * It implements immutable state updates with pub/sub pattern.
 * 
 * @module @gridkit/core/state
 */

export { createStore } from './create-store';
export type { Store } from './types';