/**
 * Table Event Integration.
 *
 * Provides integration between table instances and the event system.
 * Includes the main event bridge and integration utilities.
 *
 * @module @gridkit/core/events/integration
 */

export { createTableEventBridge } from './table-event-bridge';
export type { TableEventBridge } from '../types/event-bridge';
