/**
 * Row module for GridKit.
 *
 * This module provides functionality for creating and managing table rows.
 * Rows represent individual data records with cells, selection state, and hierarchy.
 *
 * @module @gridkit/core/row
 */

export { createRow } from './create-row';
export { buildRowModel } from './create-row';
export type { RowConfig, RowModelConfig } from './create-row';
